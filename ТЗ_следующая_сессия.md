# ТЗ на следующую сессию — СНТ «Берёзка» (berezka-php)

> Составлено 2026-06-12. Текущая сессия обнуляется. Это план продолжения.

## 0. Текущее состояние (что уже сделано)

**Боевой сайт — `berezka-php` (PHP MVC) на http://localhost:8080.** React-версия заморожена.
Запуск: `start-snt.bat` (MySQL :3306 + Apache :80/:8080). MySQL обязателен.
Стек: PHP + **mysqli** + сессии + CSRF (НЕ PDO, НЕ Bearer). Шаблоны `templates/`, рендер `renderTemplate()`.
Тестовые входы: chairman/Test1235, treasurer/Test1236, secretary/Test1237, member/Test1238 (admin/Test1234, guest/Test1239).

**Готово в этой сессии:**
- Перенос на PHP-версию на :8080, удалены старые бэкапы (snt-berezka, …копия, .rar).
- Раздел **Бюджет** (`/admin/budget`, модель `Budget`, таблицы `budget_items`/`budget_expenses`).
- **Роли**: казначей(3)→Финансы+Бюджет; секретарь(4)→Собрания+Голосования+Реестр+Документы; дашборд→все правление. Хелперы в `utils.php`: `isAdmin`[1,2], `isBoard`[1,2,3,4], `canFinance`[1,2,3], `canMeetings`[1,2,4].
- **Смета для членов** (`/budget`, read-only) + виджеты бюджета на дашбордах.
- Связка **Финансы↔Бюджет** (вкладки, `templates/partials/finance_tabs.html.php`).
- **Реестр членов** (`/admin/registry` + CSV-экспорт `/admin/registry/export`) + **архив документов** (`/admin/documents`, члены видят публичные на `/protocols`).
- **Уведомления** (таблица `notifications`, модель `Notification`, колокольчик в навигации, `/notifications`, авто-уведомления при собрании/голосовании, кнопка «Напомнить о взносах» `/admin/remind-fees`).

## 1. ПЕРВЫМ ДЕЛОМ — удалить React-версию
Папка `D:\Проекты claude\сайт снт\snt-berezka — копия (2)` (React, заморожена).
**Перед удалением:** скопировать в `berezka-php\` то, что ещё не перенесено: `.claude\` (скилы/настройки), при необходимости свежий `CLAUDE.md`. `start-snt.bat`/`stop-snt.bat` и `CLAUDE.md` — проверить, что копии уже есть в berezka-php.
Удалять, когда рабочая директория НЕ внутри этой папки (иначе оболочка падает). Данные в БД (MySQL) общие — не затрагиваются.

## 2. Функция 5 — Обращения в правление (тикеты) ✅ ГОТОВО (2026-06-12)
Реализовано и проверено end-to-end (HTTP + контроль доступа + кириллица в БД):
- Миграция `database/migrations/003_tickets.php` — таблицы `tickets` и `ticket_replies` (созданы).
- Модель `src/models/Ticket.php` (`ensureTable`, `create`, `forUser`, `all`, `find`, `replies`, `addReply`, `setStatus`, `openCount`, константы `STATUSES`/`STATUS_LABELS`).
- Член: `/tickets` — `MemberController::tickets()`: форма создания + список своих + просмотр `?id=N` с перепиской и ответом (ответ члена → статус `in_progress`).
- Правление [1,2]: `/admin/tickets` — `AdminController::tickets()`: список с фильтром по статусу, просмотр `?id=N`, ответ (→ `answered` + уведомление члену через `Notification::create`), смена статуса (+уведомление).
- Маршруты `/tickets` и `/admin/tickets` в `router.php`. Навигация: член — «✉️ Обращения»; правление — «✉️ Обращения» с бейджем `Ticket::openCount()`.
- Шаблоны `templates/pages/member/tickets.html.php`, `templates/pages/admin/tickets.html.php`.
- Проверено: член→403 на /admin/tickets; POST ответа→302; статусы и уведомления пишутся; кириллица в utf8mb4 ок.

## 3. Принятые улучшения (делать после Ф.5)
1. **Календарь событий СНТ** — собрания, даты оплат, субботники, плановые отключения единой лентой/календарём на главной.
2. **Годовой финотчёт + выгрузка в Excel/CSV** — взносы/расходы/бюджет одним отчётом для ревизкомиссии и общего собрания (по образцу `/admin/registry/export`).
3. **Голосование по ФЗ-217: кворум и протокол** — подсчёт кворума (% от числа членов), выгрузка протокола итогов, варианты «за/против/воздержался». Расширить существующий раздел голосований (`Vote`, `/admin/votes`, `/votes`).
4. **Самостоятельный профиль участника** — член редактирует телефон/ФИО/аватар; правление видит актуальные контакты в реестре. Таблица `users` уже имеет `phone`, `avatar_url`.

Отклонено пользователем: подача показаний счётчика, PDF-квитанции, онлайн-оплата (сайт информационный, без платежей — безопасность).

## 4. Правила работы (важно)
- Сайт в тестовом режиме, данные тестовые — потеря не критична. Фокус: код, целостность, логика, визуал.
- **Бюджет токенов:** ориентироваться на статус-бар (светофор). Не начинать шаг, который не успеть закончить целиком. Дробить на проверяемые шаги, после каждого — проверка.
- Параллельные агенты / Workflow для тестирования — по предпочтению пользователя (скилы snt-test/snt-parallel-test).
- После правок PHP синхронизация в htdocs НЕ нужна (Apache читает berezka-php напрямую).
